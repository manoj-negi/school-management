// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"go-seed/ent/class"
	"go-seed/ent/student"
	"go-seed/ent/studentattendance"
	"go-seed/ent/subject"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// StudentAttendance is the model entity for the StudentAttendance schema.
type StudentAttendance struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// StudentID holds the value of the "student_id" field.
	StudentID uuid.UUID `json:"student_id,omitempty"`
	// ClassID holds the value of the "class_id" field.
	ClassID int `json:"class_id,omitempty"`
	// SubjectID holds the value of the "subject_id" field.
	SubjectID *int `json:"subject_id,omitempty"`
	// Date holds the value of the "date" field.
	Date time.Time `json:"date,omitempty"`
	// Status holds the value of the "status" field.
	Status studentattendance.Status `json:"status,omitempty"`
	// Remarks holds the value of the "remarks" field.
	Remarks *string `json:"remarks,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the StudentAttendanceQuery when eager-loading is set.
	Edges        StudentAttendanceEdges `json:"edges"`
	selectValues sql.SelectValues
}

// StudentAttendanceEdges holds the relations/edges for other nodes in the graph.
type StudentAttendanceEdges struct {
	// Student holds the value of the student edge.
	Student *Student `json:"student,omitempty"`
	// Class holds the value of the class edge.
	Class *Class `json:"class,omitempty"`
	// Subject holds the value of the subject edge.
	Subject *Subject `json:"subject,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// StudentOrErr returns the Student value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StudentAttendanceEdges) StudentOrErr() (*Student, error) {
	if e.Student != nil {
		return e.Student, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: student.Label}
	}
	return nil, &NotLoadedError{edge: "student"}
}

// ClassOrErr returns the Class value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StudentAttendanceEdges) ClassOrErr() (*Class, error) {
	if e.Class != nil {
		return e.Class, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: class.Label}
	}
	return nil, &NotLoadedError{edge: "class"}
}

// SubjectOrErr returns the Subject value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e StudentAttendanceEdges) SubjectOrErr() (*Subject, error) {
	if e.Subject != nil {
		return e.Subject, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: subject.Label}
	}
	return nil, &NotLoadedError{edge: "subject"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StudentAttendance) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case studentattendance.FieldID, studentattendance.FieldClassID, studentattendance.FieldSubjectID:
			values[i] = new(sql.NullInt64)
		case studentattendance.FieldStatus, studentattendance.FieldRemarks:
			values[i] = new(sql.NullString)
		case studentattendance.FieldDate:
			values[i] = new(sql.NullTime)
		case studentattendance.FieldStudentID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StudentAttendance fields.
func (_m *StudentAttendance) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case studentattendance.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case studentattendance.FieldStudentID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field student_id", values[i])
			} else if value != nil {
				_m.StudentID = *value
			}
		case studentattendance.FieldClassID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field class_id", values[i])
			} else if value.Valid {
				_m.ClassID = int(value.Int64)
			}
		case studentattendance.FieldSubjectID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field subject_id", values[i])
			} else if value.Valid {
				_m.SubjectID = new(int)
				*_m.SubjectID = int(value.Int64)
			}
		case studentattendance.FieldDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field date", values[i])
			} else if value.Valid {
				_m.Date = value.Time
			}
		case studentattendance.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = studentattendance.Status(value.String)
			}
		case studentattendance.FieldRemarks:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field remarks", values[i])
			} else if value.Valid {
				_m.Remarks = new(string)
				*_m.Remarks = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StudentAttendance.
// This includes values selected through modifiers, order, etc.
func (_m *StudentAttendance) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryStudent queries the "student" edge of the StudentAttendance entity.
func (_m *StudentAttendance) QueryStudent() *StudentQuery {
	return NewStudentAttendanceClient(_m.config).QueryStudent(_m)
}

// QueryClass queries the "class" edge of the StudentAttendance entity.
func (_m *StudentAttendance) QueryClass() *ClassQuery {
	return NewStudentAttendanceClient(_m.config).QueryClass(_m)
}

// QuerySubject queries the "subject" edge of the StudentAttendance entity.
func (_m *StudentAttendance) QuerySubject() *SubjectQuery {
	return NewStudentAttendanceClient(_m.config).QuerySubject(_m)
}

// Update returns a builder for updating this StudentAttendance.
// Note that you need to call StudentAttendance.Unwrap() before calling this method if this StudentAttendance
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *StudentAttendance) Update() *StudentAttendanceUpdateOne {
	return NewStudentAttendanceClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the StudentAttendance entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *StudentAttendance) Unwrap() *StudentAttendance {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: StudentAttendance is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *StudentAttendance) String() string {
	var builder strings.Builder
	builder.WriteString("StudentAttendance(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("student_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.StudentID))
	builder.WriteString(", ")
	builder.WriteString("class_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ClassID))
	builder.WriteString(", ")
	if v := _m.SubjectID; v != nil {
		builder.WriteString("subject_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("date=")
	builder.WriteString(_m.Date.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	if v := _m.Remarks; v != nil {
		builder.WriteString("remarks=")
		builder.WriteString(*v)
	}
	builder.WriteByte(')')
	return builder.String()
}

// StudentAttendances is a parsable slice of StudentAttendance.
type StudentAttendances []*StudentAttendance
