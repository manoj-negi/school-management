// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"go-seed/ent/teacher"
	"go-seed/ent/teacherattendance"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// TeacherAttendance is the model entity for the TeacherAttendance schema.
type TeacherAttendance struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// TeacherID holds the value of the "teacher_id" field.
	TeacherID uuid.UUID `json:"teacher_id,omitempty"`
	// Date holds the value of the "date" field.
	Date time.Time `json:"date,omitempty"`
	// Status holds the value of the "status" field.
	Status teacherattendance.Status `json:"status,omitempty"`
	// Remarks holds the value of the "remarks" field.
	Remarks *string `json:"remarks,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TeacherAttendanceQuery when eager-loading is set.
	Edges        TeacherAttendanceEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TeacherAttendanceEdges holds the relations/edges for other nodes in the graph.
type TeacherAttendanceEdges struct {
	// Teacher holds the value of the teacher edge.
	Teacher *Teacher `json:"teacher,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// TeacherOrErr returns the Teacher value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TeacherAttendanceEdges) TeacherOrErr() (*Teacher, error) {
	if e.Teacher != nil {
		return e.Teacher, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: teacher.Label}
	}
	return nil, &NotLoadedError{edge: "teacher"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TeacherAttendance) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case teacherattendance.FieldID:
			values[i] = new(sql.NullInt64)
		case teacherattendance.FieldStatus, teacherattendance.FieldRemarks:
			values[i] = new(sql.NullString)
		case teacherattendance.FieldDate:
			values[i] = new(sql.NullTime)
		case teacherattendance.FieldTeacherID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TeacherAttendance fields.
func (_m *TeacherAttendance) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case teacherattendance.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case teacherattendance.FieldTeacherID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field teacher_id", values[i])
			} else if value != nil {
				_m.TeacherID = *value
			}
		case teacherattendance.FieldDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field date", values[i])
			} else if value.Valid {
				_m.Date = value.Time
			}
		case teacherattendance.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = teacherattendance.Status(value.String)
			}
		case teacherattendance.FieldRemarks:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field remarks", values[i])
			} else if value.Valid {
				_m.Remarks = new(string)
				*_m.Remarks = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TeacherAttendance.
// This includes values selected through modifiers, order, etc.
func (_m *TeacherAttendance) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTeacher queries the "teacher" edge of the TeacherAttendance entity.
func (_m *TeacherAttendance) QueryTeacher() *TeacherQuery {
	return NewTeacherAttendanceClient(_m.config).QueryTeacher(_m)
}

// Update returns a builder for updating this TeacherAttendance.
// Note that you need to call TeacherAttendance.Unwrap() before calling this method if this TeacherAttendance
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TeacherAttendance) Update() *TeacherAttendanceUpdateOne {
	return NewTeacherAttendanceClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TeacherAttendance entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TeacherAttendance) Unwrap() *TeacherAttendance {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TeacherAttendance is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TeacherAttendance) String() string {
	var builder strings.Builder
	builder.WriteString("TeacherAttendance(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("teacher_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TeacherID))
	builder.WriteString(", ")
	builder.WriteString("date=")
	builder.WriteString(_m.Date.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	if v := _m.Remarks; v != nil {
		builder.WriteString("remarks=")
		builder.WriteString(*v)
	}
	builder.WriteByte(')')
	return builder.String()
}

// TeacherAttendances is a parsable slice of TeacherAttendance.
type TeacherAttendances []*TeacherAttendance
